#!/usr/bin/env python
# -*- coding: utf-8 -*-

# Multiple Modules: Store the User class in one module, adn store the
# Privileges and Admin classes in a separate module. In a separate file,
# create an Admin instance and call show_privileges() show that everything
# is still working correctly.
