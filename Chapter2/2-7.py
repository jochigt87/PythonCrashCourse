#!/usr/bin/env python
# -*- coding: utf-8 -*-

# Store a person's name, and include some whitespace characters at
# the beggining and end of the name. Make sure you use each characters
# combination, "\t" and "\n", at least once.

# Print the name once, so the whitespace around the name is displayed
# Then print the name using each of the three stripping fuctions,
#  lstrip(), rstrip(), and strip()


name = "jochimin 1"

print name.lstrip()
print name.rstrip()
print name.strip()


